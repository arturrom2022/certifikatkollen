{
  "name": "kompetensspar-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./apps/* run dev",
    "build": "pnpm -r --filter ./apps/* run build",
    "start": "pnpm -r --parallel --filter ./apps/* run start",
    "lint": "pnpm -r --filter ./apps/* run lint",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
